import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'agendaST-0222',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
